---
name: plain-language-letters
description: >
  Referência: DESCONTINUADA — use `/client-letter` para correspondência de
  rotina ou `/status client` para atualizações substantivas. Dividida em duas
  skills mais focadas durante a reconstrução v2. Mantida como
  redirecionamento para migração.
user-invocable: false
---

> **Adaptação BR (não oficial).** Skill descontinuada. Padrão de linguagem simples para o(a) assistido(a) do NPJ: nível de leitura ensino fundamental II, sem juridiquês ("outrossim", "destarte", "consoante", "vossa excelência" em carta ao(à) assistido(a) são banidos). Elementos obrigatórios: o que aconteceu, o que vem a seguir, o que o(a) assistido(a) precisa fazer, como falar com o NPJ.

# [DESCONTINUADA] Cartas em Linguagem Simples → veja `/client-letter` e `/status client`

Esta skill foi dividida durante a reconstrução v2:

- **Correspondência de rotina** (confirmações de atendimento, pedidos de
  documentos, atualizações breves "protocolamos") → `skills/client-letter/` —
  use `/client-letter [tipo]`

- **Atualizações substantivas de status ao(à) assistido(a)** → `skills/status/`
  em modo voltado ao(à) assistido(a) — use `/status client`

Ambas aplicam os padrões de linguagem simples (nível de leitura, sem juridiquês)
do CLAUDE.md.

Veja os respectivos arquivos SKILL.md para os fluxos completos.
